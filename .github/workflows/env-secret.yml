name: Environment Password Demo

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest

    environment: dev   # 👈 links job to the environment

    steps:
      - name: Check password exists (safe test)
        run: echo "Password length is ${#PASSWORD}"
        env:
          PASSWORD: ${{ secrets.PASSWORD }}